subsection MetricFlowSystem<1, 3>
  set Finite element degree                = 2
  set Number of global refinement          = 5
  set Number of refinement cycles          = 1
  set Output filename                      = solution
  set Output directory                     = output
  set Problem constants                    = 1.0
  set Theta Boundary (stability parameter) = 0
  set Theta (penalty parameter)            = 0
  set Use direct solver                    = true
  set Verbosity (console depth)            = 1
  set Numerical flux type                  = HLL
  subsection ARKOde parameters
    set Final time                        = 50
    set Initial time                      = 0
    set Time interval between each output = 1
    subsection Error control
      set Absolute error tolerance = 1e-8
      set Relative error tolerance = 1e-6
    end
    subsection Running parameters
      set Anderson-acceleration subspace         = 3
      set Implicit function is linear            = false
      set Implicit function is time independent  = false
      set Initial step size                      = 1e-2
      set Mass is time independent               = true
      set Maximum number of nonlinear iterations = 10
      set Maximum order of ARK                   = 5
      set Minimum step size                      = 1e-06
    end
  end
end
subsection Metric Flow Parameters
  set Density (rho)                           = 1.06
  set Elastic modulus of the vessel wall (E) = 0.04
  set Reference cross-sectional area (a0)     = 3.14e-4
  set Reference pressure (p0)                 = 9.4666666e+4
  set Reference radius (r0)                   = 1e-2
  set Tube law exponent (m)                   = 0.5
  set Viscosity coefficient (mu)           = 1
end
subsection Functions
  set Exact solution = 0.1*a0*sin(2*PI*x)*cos(2*PI*t/5) + a0; -0.02*a0*sin(2*PI*t/5)*cos(2*PI*x)
  set Initial condition = 0.1*a0*sin(2*PI*x) + a0; 0
  set RHS expression = PI*a0*(0.04*a0*(0.1*sin(2*PI*x)*cos(2*PI*t/5) + 1)*sin(2*PI*x) - 0.004*a0*cos(2*PI*t/5)*cos(2*PI*x)^2 - 0.04*sin(2*PI*x))*sin(2*PI*t/5); (-a0*rho*(0.1*sin(2*PI*x)*cos(2*PI*t/5) + 1)*(0.0008*PI*a0*sin(2*PI*t/5)^2*sin(2*PI*x) + 0.02*mu*sin(2*PI*t/5) + 0.008*PI*cos(2*PI*t/5)) + 0.2*PI*m*E*(0.1*sin(2*PI*x)*cos(2*PI*t/5) + 1)^m*cos(2*PI*t/5))*cos(2*PI*x)/(rho*(0.1*sin(2*PI*x)*cos(2*PI*t/5) + 1))
end
